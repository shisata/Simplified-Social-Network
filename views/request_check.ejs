<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="stylesheets/style.css">
  <title>Incoming Requests</title>
</head>

<% if(typeof errors!='undefined'){ %>
<%      errors.forEach(function(error) { %>
<p class="error"><%=  error.msg           %></p>
<% }) %>
<% } %>

<% if(success_msg!=''){ %>
<p class="error"><%=  success_msg %> </p>         
<% } %>

<% if(error_msg!=''){ %>
<p class="error"><%=  error_msg %> </p>         
<% } %>

<!-- Lists Incoming Requests -->
<!-- TODO: List incoming requests in a proper format -->
<body>

    <% requests.forEach(function(user) { %>
        <li>
            Sender
            <%= user.sender_id %>
            Receiver
            <%= user.receiver_id %>                  
        </li>

        <!-- TODO: Add Action buttons for for app.post(/friends/add/manage) -->
    
        <!-- ADD Response and Friend ID fields (USED in add/manage) -->


    <% }); %>

</body>


</html>